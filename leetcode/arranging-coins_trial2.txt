class Solution:
    def arrangeCoins(self, n: int) -> int:
        count = 0
        r = n
        for i in range(1,n+1):
            r -= i
            if r >= 0:
                count +=1
            else:
                return count
        return count